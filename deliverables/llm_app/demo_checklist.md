# 实习演示清单

- [ ] 启动 `scripts/run_cpu_demo.sh cached`，展示最快链路；
- [ ] 切换 `replay`，展示 Student/FIRST 路由；
- [ ] 切换 `cpu`，展示真实 MiniLM checkpoint；
- [ ] 在 Streamlit 提交一个 Query；
- [ ] 调用 MCP `search` 和 `get_runtime_metrics`；
- [ ] 连续两次使用同一 user_id，展示 A/B 分桶稳定；
- [ ] 提交一次 click/like 反馈；
- [ ] 打开 `/metrics/prometheus`，展示调用率和延迟；
- [ ] 说明 RTX 4090 延迟来自锁定离线实测，当前演示为 CPU/replay。
